# GRPO training dependencies for MultihopSpatial.
# Verified against CUDA 12.8 on 8x A100 80GB.
#
#   pip install -r requirements.txt
#
# Install flash-attn last, after everything else is in place:
#   pip install flash-attn==2.7.2.post1 --no-build-isolation

torch==2.8.0
torchvision==0.23.0
transformers==4.57.0
accelerate==1.6.0
trl==0.23.1
peft==0.16.0
deepspeed==0.18.6
datasets==4.5.0
huggingface_hub==0.36.2
qwen-vl-utils==0.0.14
pillow==12.1.1
ujson==5.11.0
numpy==2.2.6

# Optional: only needed when training with --wandb
wandb==0.25.0
